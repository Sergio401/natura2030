---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Page from '../../../themes/v3-costa-interfaz/Page.astro';
import { getContent } from '../../../data';

const t = getContent('en');
---

<BaseLayout lang="en" version="v3" title={`${t.meta.siteName} — The Coastline`} description={t.meta.description}>
  <Page locale="en" />
</BaseLayout>
